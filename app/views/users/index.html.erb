<div class="container">
    <h2>Users Index</h2>
    <hr>
    <ul>
      <% @users.each do |u| %>
        <li>
          <%= link_to u.name, user_path(u.id) %>
        </li>
      <% end %>
    </ul>
</div>
